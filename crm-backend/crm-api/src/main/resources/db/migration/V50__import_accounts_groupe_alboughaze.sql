SET QUOTED_IDENTIFIER ON;
SET ANSI_NULLS ON;
GO

-- V50 : import depuis GROUPE_ALBOUGHAZE.dbo désactivé (base externe non disponible en dev)
PRINT 'V50 : skipped (external DB GROUPE_ALBOUGHAZE not available)';
GO
