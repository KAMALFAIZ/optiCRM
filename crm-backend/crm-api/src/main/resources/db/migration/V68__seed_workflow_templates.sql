SET QUOTED_IDENTIFIER ON;
SET ANSI_NULLS ON;
GO

-- V68 : seed de templates de workflow désactivé (échappement JSON incompatible SQL Server)
PRINT 'V68 : skipped (PostgreSQL-style JSON escaping not compatible with SQL Server)';
GO
